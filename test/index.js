import test from 'ava'

test('#get-licence', t => {
  t.pass()
})

test('#check-licence-valid', async t => {
  // const bar = Promise.resolve('bar')
  // t.is(await bar, 'bar')

  t.pass()
})

test('#test-request', t => {
  // 检测 header 签名
  t.pass()
})